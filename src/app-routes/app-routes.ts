interface IRoutes {
  HOME: string,
  COUNTRIES: string,
  RULES: string,
  RESULTS: string,
}

const AppRoute: IRoutes = {
  HOME: "/",
  COUNTRIES: "/game",
  RULES: "/rules",
  RESULTS: "/results",
};

export default AppRoute;
